{
  "name": "pso-server-dashboard",
  "version": "0.1.0",
  "description": "Static dashboard + safe-endpoint proxy for a private newserv PSO server",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node --watch server.js"
  },
  "dependencies": {
    "@aws-sdk/client-cost-explorer": "^3.1071.0",
    "express": "^4.21.2",
    "ws": "^8.21.0"
  },
  "engines": {
    "node": ">=20"
  }
}
